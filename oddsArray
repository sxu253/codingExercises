7.4: oddsArray
Write code that creates an array named odds and stores all odd numbers between -6 and 38 into it using a for loop. 
Make the array's size exactly large enough to store the numbers.

ArrayList<Integer> storeOdds = new ArrayList<Integer>();
for (int i = -5; i < 38; i += 2) {
    storeOdds.add(i);
}
int [] odds = new int[storeOdds.size()];

for (int k = 0; k < storeOdds.size(); k++){
    odds[k] = storeOdds.get(k);
}
